{
  "name": "@task-pipeline/codegraph",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@optave/codegraph": "^3.17.0"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
